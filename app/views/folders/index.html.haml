%h1 Listing folders

%table
  %tr
    %th Name
    %th Group
    %th
    %th
    %th

  - @folders.each do |folder|
    %tr
      %td= folder.name
      %td= folder.group_id
      %td= link_to 'Show', folder
      %td= link_to 'Edit', edit_folder_path(folder)
      %td= link_to 'Destroy', folder, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Folder', new_folder_path
